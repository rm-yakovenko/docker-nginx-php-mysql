sudo: required

services:
  - docker

script:
  - cp .env.dist .env
  - XDEBUG=1 docker-compose build
  - docker/console php -i
  - docker-compose up -d
  - curl -f localhost:8080
  - docker/console id
  - docker/redis-cli info